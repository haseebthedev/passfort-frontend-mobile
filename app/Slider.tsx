import React from "react";
import { StyleSheet } from "react-native";
import { ArcSlider } from "@/components";

const Slider = () => {
  return <ArcSlider />;
};

export default Slider;

const styles = StyleSheet.create({});
